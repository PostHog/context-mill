# Orchestrator step-skill: fetched by task agents, never surfaced as a command.
# The framework matrix is borrowed from the canonical integration group.
type: docs-only
template: description.md
description: Install the PostHog SDK with the project's package manager
tags: [orchestrator, install]
cli:
  role: internal
variants_from: integration
