type: skill
template: description.md
category: migrate
description: Migrate an existing analytics or feature-flag vendor to PostHog. Replaces SDK call sites in-place, removes the source package, and writes a migration report. Replacement-only, doesn't adds new instrumentation.
tags: [migration]
references:
  preamble: "**Read ONLY this file.** Do not read any other reference file until this one tells you to."
shared_docs: []
variants:
  - id: statsig
    display_name: Statsig → PostHog
    tags: [migration, statsig]
    docs_urls: []
  - id: mixpanel
    display_name: Mixpanel → PostHog
    tags: [migration, mixpanel]
    # Everything the agent needs to perform the migration is on disk:
    #   - references/sdk-reference.md (Mixpanel API surface)
    #   - references/mapping.md       (Mixpanel → PostHog mapping + PostHog SDK quick reference)
    # No WebFetch at runtime; no external doc dependencies.
    docs_urls: []
